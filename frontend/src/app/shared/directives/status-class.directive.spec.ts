import { StatusClassDirective } from './status-class.directive';

describe('StatusClassDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusClassDirective();
    expect(directive).toBeTruthy();
  });
});
